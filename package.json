{
  "name": "action-recorder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension: record UI actions + correlated network requests",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
